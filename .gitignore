# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Raw snapshots & processed artifacts are regenerated by the pipeline (not versioned).
# outputs/ (the plots) ARE tracked so the results are visible in the repo.
data/raw/*
data/processed/*
!**/.gitkeep

# OS / editor / tooling
.DS_Store
.idea/
.vscode/
.claude/
